# Optional Trading Dependencies
# Install these separately if needed, but note conflicts with core requirements

# Alpaca Trading (conflicts with Prefect due to websockets versions)
# alpaca-trade-api==3.1.1

# Alternative: Use newer Alpaca SDK (compatible with Prefect)
# alpaca-py>=0.12.0

# Installation instructions:
# 1. For core system with Prefect: pip install -r requirements.txt
# 2. For Alpaca integration (without Prefect): pip install alpaca-trade-api==3.1.1
# 3. For newer Alpaca + Prefect: pip install alpaca-py>=0.12.0 (requires code updates)